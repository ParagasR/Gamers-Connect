<a href='/posts/{{id}}'>
    <div class="card">
        <div class="card-header content">
            <h1>{{title}}</h1>
            <h1>{{user.username}}</h1>

        </div>
        <div class="card-content content">
            <h1>{{content}}</h1>
        </div>


    </div>
</a>