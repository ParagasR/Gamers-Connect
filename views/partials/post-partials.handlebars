<div id="post-card" class="card is-three-quarters">
    <div id="post-card-header" class="card-header content">
        <h1>{{title}}</h1>
        <h2>{{user.username}}</h2>
    
    </div>
    <div id="card-post" class="card-content content">
        <h3>{{content}}</h3>
    </div>

</div>