<div class="card">
     <div class="card-header content">
       <h1>{{post.title}}</h1>
       <h1>{{post.user.username}}</h1>

    </div>
    <div class="card-content content">
        <h1>{{post.content}}</h1>
    </div>

</div>